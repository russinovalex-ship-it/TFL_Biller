# Пошаговая инструкция по установке и запуску Telegram-бота учёта рабочего времени

## 📋 Что вам понадобится

- Python 3.9 или новее
- Токен Telegram-бота
- 10-15 минут времени

---

## Шаг 1: Установка Python

### Windows:
1. Скачайте Python с официального сайта: https://www.python.org/downloads/
2. Запустите установщик
3. ⚠️ **ВАЖНО**: Обязательно поставьте галочку **"Add Python to PATH"**
4. Нажмите "Install Now"
5. После установки откройте командную строку (Win+R → `cmd` → Enter)
6. Проверьте установку:
   ```bash
   python --version
   ```
   Должна появиться версия Python (например, `Python 3.11.5`)

### Linux (Ubuntu/Debian):
```bash
sudo apt update
sudo apt install python3 python3-pip
python3 --version
```

### macOS:
```bash
brew install python3
python3 --version
```

---

## Шаг 2: Создание Telegram-бота

1. Откройте Telegram
2. Найдите бота **@BotFather** (официальный бот от Telegram)
3. Отправьте команду `/newbot`
4. BotFather спросит имя бота. Введите, например: `Учёт времени юриста`
5. BotFather попросит username. Введите уникальное имя, заканчивающееся на `bot`, например: `my_lawyer_timetracker_bot`
6. BotFather отправит вам **токен** — это длинная строка вида:
   ```
   1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567890
   ```
7. **⚠️ СКОПИРУЙТЕ И СОХРАНИТЕ ЭТОТ ТОКЕН!** Он понадобится на следующем шаге

---

## Шаг 3: Настройка файлов бота

### 3.1. Создайте папку для бота

**Windows (командная строка):**
```bash
mkdir C:\lawyer_bot
cd C:\lawyer_bot
```

**Linux/macOS (терминал):**
```bash
mkdir ~/lawyer_bot
cd ~/lawyer_bot
```

### 3.2. Скопируйте файлы

Поместите в созданную папку файлы:
- `lawyer_timetracker_bot.py` (основной код)
- `requirements.txt` (список библиотек)

### 3.3. Укажите токен бота

1. Откройте файл `lawyer_timetracker_bot.py` в любом текстовом редакторе (Блокнот, Notepad++, VS Code)
2. Найдите строку 33:
   ```python
   BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"
   ```
3. Замените `YOUR_BOT_TOKEN_HERE` на токен от BotFather:
   ```python
   BOT_TOKEN = "1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567890"
   ```
4. Сохраните файл

---

## Шаг 4: Установка библиотек

Откройте командную строку/терминал в папке с ботом и выполните:

**Windows:**
```bash
pip install -r requirements.txt
```

**Linux/macOS:**
```bash
pip3 install -r requirements.txt
```

Процесс займёт 1-2 минуты. Вы увидите установку библиотек: `python-telegram-bot`, `pandas`, `openpyxl`.

---

## Шаг 5: Запуск бота

В той же командной строке/терминале выполните:

**Windows:**
```bash
python lawyer_timetracker_bot.py
```

**Linux/macOS:**
```bash
python3 lawyer_timetracker_bot.py
```

### ✅ Если всё настроено правильно:

Вы увидите сообщения в консоли:
```
INFO - База данных инициализирована
INFO - Бот запущен!
```

**🎉 Поздравляю! Бот работает!**

---

## Шаг 6: Использование бота

1. Откройте Telegram
2. Найдите вашего бота по username (который вы создали в BotFather)
3. Нажмите **"Start"** или отправьте `/start`
4. Бот отправит вам список команд

### Первые действия:

```
/add_client → добавьте клиента
/add_project → создайте проект для клиента
/work → начните работу
/stop → завершите работу
/today → посмотрите отчёт
```

---

## 🛑 Остановка бота

Чтобы остановить бота, нажмите в командной строке:
```
Ctrl + C
```

---

## 🚀 Автоматический запуск (удобный способ)

### Для Windows:

1. Создайте в папке с ботом файл `start_bot.bat`
2. Откройте его Блокнотом и вставьте:
   ```batch
   @echo off
   cd /d %~dp0
   python lawyer_timetracker_bot.py
   pause
   ```
3. Сохраните
4. Теперь запускайте бота **двойным кликом** по файлу `start_bot.bat`

### Для Linux/macOS:

1. Создайте файл `start_bot.sh`:
   ```bash
   nano start_bot.sh
   ```
2. Вставьте:
   ```bash
   #!/bin/bash
   cd "$(dirname "$0")"
   python3 lawyer_timetracker_bot.py
   ```
3. Сохраните (Ctrl+O, Enter, Ctrl+X)
4. Сделайте исполняемым:
   ```bash
   chmod +x start_bot.sh
   ```
5. Запускайте:
   ```bash
   ./start_bot.sh
   ```

---

## 🌐 Запуск на сервере (для продвинутых)

Если вы хотите, чтобы бот работал **круглосуточно** на сервере (например, на Timeweb):

### 1. Подключитесь к серверу по SSH:
```bash
ssh root@ваш_IP_адрес
```

### 2. Установите Python:
```bash
apt update
apt install python3 python3-pip -y
```

### 3. Создайте папку и загрузите файлы:
```bash
mkdir /root/lawyer_bot
cd /root/lawyer_bot
```

Загрузите файлы через SFTP или используйте `nano` для создания файлов вручную.

### 4. Установите библиотеки:
```bash
pip3 install -r requirements.txt
```

### 5. Запустите бота в фоновом режиме с помощью screen:
```bash
screen -S lawyer_bot
python3 lawyer_timetracker_bot.py
```

### 6. Отключитесь от screen:
Нажмите: `Ctrl+A`, затем `D`

Бот продолжит работать даже после закрытия SSH!

### Вернуться к боту:
```bash
screen -r lawyer_bot
```

### Автозапуск при перезагрузке сервера:

Создайте systemd-сервис:

```bash
nano /etc/systemd/system/lawyer-bot.service
```

Вставьте:
```ini
[Unit]
Description=Lawyer Time Tracker Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/lawyer_bot
ExecStart=/usr/bin/python3 /root/lawyer_bot/lawyer_timetracker_bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Активируйте:
```bash
systemctl daemon-reload
systemctl enable lawyer-bot
systemctl start lawyer-bot
systemctl status lawyer-bot
```

---

## ⚠️ Решение проблем

### Ошибка: `No module named 'telegram'`
**Решение:**
```bash
pip install python-telegram-bot --upgrade
```

### Ошибка: `Unauthorized` или `Invalid token`
**Причина:** Неправильный токен в коде
**Решение:** Проверьте, что токен в файле `lawyer_timetracker_bot.py` точно совпадает с токеном от BotFather (без пробелов!)

### Бот не отвечает в Telegram
**Проверьте:**
1. Бот запущен (видите "Бот запущен!" в консоли)
2. Интернет-соединение работает
3. Токен указан правильно
4. В BotFather нажали "Start" в боте

### Ошибка: `python is not recognized`
**Причина:** Python не добавлен в PATH
**Решение:** Переустановите Python с галочкой "Add Python to PATH"

### База данных повреждена
**Решение:** Удалите файл `lawyer_timetracker.db` и перезапустите бота
⚠️ **Внимание:** Все данные будут потеряны!

---

## 💾 Резервное копирование данных

Все ваши данные хранятся в файле **`lawyer_timetracker.db`** в папке с ботом.

### Регулярно создавайте копии:

**Windows:**
```bash
copy lawyer_timetracker.db lawyer_timetracker.db.backup
```

**Linux/macOS:**
```bash
cp lawyer_timetracker.db lawyer_timetracker.db.backup
```

### Восстановление из резервной копии:
Просто переименуйте файл `.backup` обратно в `lawyer_timetracker.db`

---

## 📊 Структура файлов

После установки и первого запуска у вас будет:

```
lawyer_bot/
├── lawyer_timetracker_bot.py   # Основной код бота
├── requirements.txt             # Список библиотек
├── lawyer_timetracker.db        # База данных (создаётся автоматически)
├── start_bot.bat               # Скрипт запуска (опционально)
└── timetracker_*.xlsx          # Экспортированные отчёты (временные)
```

---

## 🆘 Нужна помощь?

Если возникли проблемы:

1. Проверьте версию Python: `python --version` (должна быть 3.9+)
2. Убедитесь, что все библиотеки установлены: `pip list`
3. Проверьте логи в консоли на наличие ошибок
4. Убедитесь, что токен бота указан правильно (без кавычек и пробелов)

---

## ✅ Checklist быстрой установки

- [ ] Python 3.9+ установлен
- [ ] Токен получен от @BotFather
- [ ] Папка для бота создана
- [ ] Файлы `lawyer_timetracker_bot.py` и `requirements.txt` скопированы
- [ ] Токен вставлен в код (строка 33)
- [ ] Библиотеки установлены: `pip install -r requirements.txt`
- [ ] Бот запущен: `python lawyer_timetracker_bot.py`
- [ ] В консоли видно "Бот запущен!"
- [ ] В Telegram нажали Start и бот ответил

**Готово! Бот работает и готов к использованию! 🎉**